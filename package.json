{
  "name": "ocd-js",
  "private": true,
  "version": "0.1.0",
  "description": "Opinionated backend framework workspace for OCD-JS",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b --pretty false",
    "test": "node -e \"console.log('No tests yet - placeholder')\""
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "examples/*"
    ]
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.4.5"
  }
}
